package charcters;

/**
 * Defines the distinct types of enemies that can appear in the game.
 * Each constant represents a unique enemy species, allowing for
 * categorized creation and handling of various hostile entities.
 */
public enum EnemyType {
    /** Represents a Slime enemy. */
    SLIME,
    /** Represents a Wolf enemy. */
    WOLF,
    /** Represents a Snake enemy. */
    SNAKE,
    /** Represents a Goblin enemy. */
    GOBLIN,
    /** Represents an Orc enemy. */
    ORC;
    // Additional enemy types can be added here as the game expands.
}